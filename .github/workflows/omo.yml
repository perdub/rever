name: omo
on:
  workflow_dispatch:
  schedule:
    - cron: '0/20 * * * *'
jobs:
  build:
    runs-on: windows-latest           
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Install .NET Core
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 5.0.x
    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v1.0.2
    - name: Run
      run: dotnet run -c Debug -- --token=${{ secrets.TELEGRAM_BOT_TOKEN }} --usepixiv --pixivrefresh=${{ secrets.PIXIV_REFRESH_TOKEN }} -c -1001836193798 --rating Explicit
